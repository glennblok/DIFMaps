{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
            <tr>

            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ category.category }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ category.location }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="/{{ category.image }}" style="width:800px;height:600px;" ></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>


{% endblock %}
